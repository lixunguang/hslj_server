package router

import (
	"github.com/gin-gonic/gin"
)

func taskRouter(e *gin.Engine) {
	///g := e.Group("/quality_platform")
	///g.Use(middleware.CheckAuth)
	///taskController := controller.NewTaskController()
	//发送任务
	//g.POST("send_task", taskController.SendTask)
	///g.GET("send_task", taskController.SendTask)
}
